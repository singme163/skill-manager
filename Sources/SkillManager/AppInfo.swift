import Foundation

enum AppInfo {
    static let name = "Skill Manager"
    static let version = "1.2"
    /// TODO: 建好 GitHub 仓库后替换为真实地址（并同步 README 与公众号文章中的占位链接）。
    static let repoURL = URL(string: "https://github.com/REPLACE-ME/skill-manager")!
}
